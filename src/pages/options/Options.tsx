import React from "react";
import "@pages/options/Options.css";

const Options: React.FC = () => {
  return <div className="container text-lime-400">Flashcachy Options!!</div>;
};

export default Options;
